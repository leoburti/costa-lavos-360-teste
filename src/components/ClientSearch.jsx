import React from 'react';
// This component is being deprecated in favor of the new Popover+Command implementation inside NewRequestView.
// Keeping the file to avoid breaking imports, but it's no longer used.
const ClientSearch = () => {
  return null;
};
export default ClientSearch;